#pragma once

#include "net/minecraft/world/level/gamemode/rules/GameRuleDefinition.h"

// unsure if this should go into specific minigame/gamemode, don't know where it's used (if it's used at all)
class DegradationSequenceRuleDefinition : public GameRuleDefinition {};
